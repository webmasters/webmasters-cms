<%= form_for page, url: set_current_version_admin_page_path(page) do |f| %>
  <%= f.select :version, page_versions_for_select %>
  <%= f.submit "Use as current version" %>
<% end %>

Chosen Version:
<div id="chosen">
</div>

Current Version:
<%= render 'webmasters_cms/admin/pages/page', resource: page.current_version %>
<%= render 'webmasters_cms/admin/pages/back' %>
