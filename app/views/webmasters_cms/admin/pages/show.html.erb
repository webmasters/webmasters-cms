<%= render 'back' %>

<%= render 'actions', page: resource %>

<%= render 'page', resource: resource %>
<% content_for :title, resource.translations.find_by(page_id: params[:id]).title %>
<% content_for :meta_description, resource.translations.find_by(page_id: params[:id]).meta_description %>
