-
<%= link_to t('.delete'), admin_page_path(page),
  method: :delete,
  data: { confirm: 'Are you sure?' } %>

<%= t('.show') %>:
<%= select_tag "showPage_#{page.id}", options_from_collection_for_select(page.translations, :local_path, :language), class: "showPage" %>
