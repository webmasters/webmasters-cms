<%= link_to t('.edit'), edit_admin_page_path(page) %>
<%= link_to t('.delete'), admin_page_path(page),
  method: :delete,
  data: { confirm: 'Are you sure?' } %>

<%= t('.show') %>:
<%= select_tag 'showPage', options_from_collection_for_select(page.translations, "local_path", "language") %>
