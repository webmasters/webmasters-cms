<%= form_for resource, url: admin_page_path(resource) do |f| %>

  <%= render 'messages' %>

  <h1><%= t '.edit_page' %></h1>

  <%= render 'form', f: f %>

<% end %>

<%= render 'webmasters_cms/admin/pages/back' %>