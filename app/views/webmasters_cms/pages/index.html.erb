<h1><%= t('.allPages') %></h1>

<ul>
  <% collection.each do |page| %>
    <li>
        <%= link_to page.name, page %>
        (<%= t('.title') %>: <%= page.title %>)
      <%= render partial: 'actions', locals: {page: page} %>
    </li>
  <% end %>
</ul>